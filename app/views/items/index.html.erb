<div class="mobile-only w-75 m-auto">
  <%= render 'shared/search'%>
</div>
<% if @items.empty? %>
  <p>No hay resultados</p>
<% else %>
  <%= render "card", items: @items %>
<% end %>

<body style="background-color: #F9E2AF;">
    <!-- Aquí va el contenido de tu página -->
</body>
