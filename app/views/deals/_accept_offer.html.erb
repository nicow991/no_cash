      <%= simple_form_for(deal) do |f| %>
        <%= f.hidden_field :offer_id, value: offer.id %>
        <%= f.button :submit, "Aceptar", class: 'btn btn-primary-dark border-primary-darker my-2' %>
      <% end %>
