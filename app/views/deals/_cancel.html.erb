      <div class="col-md-6">
      <%= simple_form_for(deal, url: cancel_deal_path(deal), method: :patch) do |f| %>
        <div class="text-center">
          <%= f.button :submit, "Cancelar Trueque", class: 'btn btn-outline-danger my-2' %>
        </div>
      <% end %>
    </div>
