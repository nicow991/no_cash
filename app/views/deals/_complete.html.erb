      <%= simple_form_for(deal, url: complete_deal_path(deal), method: :patch) do |f| %>
        <div class="text-center">
          <%= f.button :submit, "Trueque concluido", class: 'btn btn-primary-dark border-primary-darker my-2' %>
        </div>
      <% end %>
