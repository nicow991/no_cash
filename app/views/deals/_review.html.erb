<button id="show-review-form" data-action='click->toggle-review#showreview'>Deja una reseña</button>
<div class="col-md-6 review-form-container d-none" data-toggle-review-target='review'>
  <%= simple_form_for(@review, url: deal_reviews_path(deal), method: :patch) do |f| %>
    <%= f.input :content, as: :text, label: false, placeholder: 'Contenido de la reseña' %>
    <%= f.input :rating, as: :select, collection: 1..5, label: false, prompt: 'Calificación' %>
    <div class="text-center">
      <%= f.button :submit, "Enviar reseña", class: 'btn btn-outline-danger my-2' %>
    </div>
    <% end %>
</div>
